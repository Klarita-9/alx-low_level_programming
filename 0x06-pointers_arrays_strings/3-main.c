#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
char s1[] = "Hello";
char s2[] = "World!";

printf($ (char *s1, s2));
printf($ char *s2, s1));
printf("%d\n", _strcat(s1, s1));
return (0);
}

